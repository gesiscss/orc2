apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gesis.fullname" . }}-test-connection"
  labels:
    {{- include "gesis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gesis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
