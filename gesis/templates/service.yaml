apiVersion: v1
kind: Service
metadata:
  name: {{ include "gesis.fullname" . }}
  labels:
    {{- include "gesis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gesis.selectorLabels" . | nindent 4 }}
